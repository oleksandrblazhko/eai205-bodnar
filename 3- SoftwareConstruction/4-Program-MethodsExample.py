class User:
    def init(self, user_id, name, phone):
        self.user_id = user_id
        self.name = user_name
        self.phone = phone

    def check_temperature_u():
        return check_temperature_m()

class Message:
    def send_message(self):
        message_id = new_message_id()
        self.sender_id = user_id
        receiver_id = input("Введіть адресата")
        text = input("Введіть текст листа")

    def give_feedback(self):
        message_id = new_message_id()
        self.sender_id = user_id
        receiver_id = 0
        text = input("Введіть текст листа")
